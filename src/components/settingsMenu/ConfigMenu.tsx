import * as React from 'react';

export interface IConfigMenuProps {
}

export const ConfigMenu = React.forwardRef((props: IConfigMenuProps) => {
  return (
    <div className='config-menu'>
        
    </div>
  );
}
)